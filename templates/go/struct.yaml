version: 3
language: go
type: file
key: model
model: entity
groups: 
  - go-rest-full
body: |  
  
  // {{ .Description }}
  type {{ .Name | pascal | singular}} struct {
    {{ range .Columns -}}       
      {{ .NameWithoutPrefix | pascal }} {{ .DataType | datatypeN .IsNullable }}
    {{ end }}
  }
  